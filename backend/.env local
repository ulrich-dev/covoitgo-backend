# ══════════════════════════════════════════════
# COVOITGO — Variables d'environnement backend
# Copiez ce fichier en .env et remplissez les valeurs
# ══════════════════════════════════════════════

# ── PostgreSQL ─────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=covoitgo
DB_USER=postgres
DB_PASSWORD=covoitgo

# ── Serveur ────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── Session / Cookie ───────────────────────────
# Générez une clé aléatoire longue (min 32 chars) :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=927a636777319eeb88b104970ef7822c9ae461c829f9a80d2f8feb791953e4c387459f3cb6800d45c8f7d7e5a43d80303aaf80abd9144a8b3efd861b1cdc0668

# ── Frontend ───────────────────────────────────
# URL de votre app React (pour CORS)
SERVER_URL=http://localhost:5000
CLIENT_URL=http://localhost:5173
# ── Email (DEV) — Mailtrap ─────────────────────
# Créez un compte gratuit sur https://mailtrap.io
# Allez dans Inboxes > votre inbox > SMTP Settings > Nodemailer
VITE_API_URL=SERVER_URL=localhost:5000
MAILTRAP_HOST=sandbox.smtp.mailtrap.io
MAILTRAP_PORT=2525
MAILTRAP_USER=231a432345259c
MAILTRAP_PASS=58314985b0e360

# ── OAuth Google ──────────────────────────────
# Obtenez vos clés sur : https://console.cloud.google.com
# URI de redirection autorisé : http://localhost:5000/api/auth/google/callback
GOOGLE_CLIENT_ID=757119208587-8s0ilr5mjag6en9pek4qt9o1mcov8rct.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# ── OAuth Facebook ────────────────────────────
# Obtenez vos clés sur : https://developers.facebook.com
# URI de redirection valide : http://localhost:5000/api/auth/facebook/callback
FACEBOOK_APP_ID=votre_facebook_app_id
FACEBOOK_APP_SECRET=votre_facebook_app_secret